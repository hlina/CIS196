<div>
	<p>
		<strong>name</strong>
		<%= @user.name %>
	</p>

	<p>
		<strong>email</strong>
		<%= @user.email %>
	</p>
</div>

<%= link_to 'Back', users_path %> 
<%= link_to 'Edit', edit_user_path(@user) %>

