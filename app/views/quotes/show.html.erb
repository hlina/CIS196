<p>
	<strong>Quote</strong>
	<%= @quote.body %>
</p>

<p>
	<strong>Category</strong>
	<%= @quote.category %>
</p>

<%= link_to 'Back', quotes_path %> 
<%= link_to 'Edit', edit_quote_path(@quote) %>